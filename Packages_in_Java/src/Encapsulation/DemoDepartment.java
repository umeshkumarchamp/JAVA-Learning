package Encapsulation;

// Why we use record classes ? 
// to create read only classes ,, simplest way to create read only
// it automatically create getters file
public record DemoDepartment(
		String name, 
		String managerName
		) {
	
}
